# v0.3.4 — Milestone 3 Phase 4: Student Home View

**Date:** 2026-07-01

## Added

### Frontend
- Identity card: animal emoji + name, personal code display with clipboard copy button
  (2 s "Gekopieerd!" / "Copied!" feedback), hint text
- 4 activity tiles rendered from `GET /student/activities`: number, title, status badge
  (`not-started` / `in-progress` / `locked`) with colour coding
- `ANIMAL_EMOJI` lookup map for all 60 animal names
- `_renderActivityTiles()` and `_copyPersonalCode()` helpers in `student.js`
- `openActivity(activity)` stub (Phase 5)
- 15 new i18n keys (NL + EN): `personal_code_label`, `personal_code_hint`, `copy`, `copied`,
  `leave`, `activity_1_title` – `activity_4_title`, `status_not_started`, `status_in_progress`,
  `status_locked`
- New CSS sections: `.identity-card`, `.animal-emoji`, `.animal-name`, `.personal-code-row`,
  `.btn-copy`, `.tiles-grid`, `.activity-tile`, `.tile-header`, `.tile-number`, `.tile-title`,
  `.status-badge` (`.not-started` / `.in-progress` / `.locked`)

## Changed

- `#view-student-home` stub replaced with real identity card + tiles grid layout
- `renderStudentHome()` is now async: shows view immediately, fetches tiles in background
- Leave button `data-i18n` changed from `btn_leave` ("Terug") to `leave` ("Verlaten")

## Notes

- No backend changes; no Alembic migration required
- All 21 tests pass
- Branch: `milestone-3-student-flow`
