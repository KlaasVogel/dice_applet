# v0.3.3 — Milestone 3 Phase 3: Frontend Join / Reconnect / Session Restore

**Date:** 2026-07-01

## Added

### Frontend
- `student.js` rewritten: module-level `session` object, real `fetch` calls for join and
  reconnect, inline error display per status code, `restoreStudentSession()` for page-load
  cookie restore, `renderStudentHome()` stub (Phase 4 will fill in identity card + tiles)
- `#join-classroom-error` and `#join-personal-error` error elements in landing section
- `#view-student-activity` empty stub section (required by router)
- `student-activity` added to `VIEWS` in `router.js`
- 4 new i18n keys (NL + EN): `error_network`, `error_join_classroom`, `error_join_full`,
  `error_join_personal`
- `app.js` init now calls `restoreStudentSession()` first; falls through to landing only if
  no valid `student_session` cookie exists

### Backend (fix)
- `StudentJoinResponse` extended with `classroom_id` and `classroom_name` — now symmetric
  with `StudentReconnectResponse` and sufficient to populate the full session object

## Notes

- No Alembic migration required
- All 21 tests pass
- Branch: `milestone-3-student-flow`
